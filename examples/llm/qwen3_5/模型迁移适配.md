我想将xhquant_llm(/data01/home/yujy/work/xhquant_llm)中已经支持的qwen3.5模型迁移到我的项目中，首先可以参考已有的skills中的一些迁移案例，了解迁移的流程和注意事项。
其次，qwen3.5模型是混合线性注意力的模型（与Qwen3Next llm部分一样，多了vit和mrope），迁移时需要特别注意模型架构的差异，确保在新项目中正确实现了混合线性注意力机制。这个在xhquant_llm中也有很多实现的文档和代码可以参考。qwen3.5模型的prefill和decoder不一样，cache也和纯全局注意力的模型不一样，迁移时需要特别注意这些细节，确保在新项目中正确实现了prefill和decoder的逻辑，以及cache的管理。
qwen3.5默认使用conda activate xhquant这个环境，transformer版本是5.2.0
先适配27B的模型，不打算做common quant，直接用GPTQ量化后的模型，/data01/home/yujy/work/gptqmodel这个库量化的，已有量化后的weights/Qwen3.5-27B-GPTQModel-self-generated-4bit-hessian-mse
官方bf16的模型权重是放在weights/Qwen3.5-27B下面的
导出脚步需要支持官方bf16和gptq量化两种权重格式，芯片只支持fp16，所以模型load后都是fp16的数据格式,官方的跑w8a8，gptq量化的跑w4a8，
构建demo脚本，验证导出的模型在新项目中的推理正确性和性能表现。
最后，迁移完成后需要进行充分的测试和验证，确保迁移后的模型在新项目中能够正确推理，并且性能表现符合预期。可以参考xhquant_llm中已有的测试脚本，构建适合Next模型的测试用例，验证迁移后的模型在新项目中的推理正确性和性能表现。
继续干，0，6, 7三张GPU供你使用，不过你导出貌似都是cpu，也用不上，hmonnx推理需要用上。
完成运行官方 BF16 -> 默认 w8a8h1_sefp和GPTQ -> 默认 w4a8h0_ssfp，测试demo，golden生成这些需要严格对齐xhquant_llm /data01/home/yujy/work/xhquant_llm/.copilot/skills下的skills

请在我不介入的情况下，完成这些，要有hf vs wrap， wrap vs front， front vs quant这些测试，由于prefill和decoder不能共用一张图，如果测试对话，需要构建两张图，这个可以参考xhquant_llm中的qwen3.5的export，他开启--valid会多阶段对比，不只比首字输出，还会比decoder多步的结果。
你在移植的过程中需要写这个脚本保证你移植的正确性，最后在组成发布的版本，包含导出和生成golden，这样是一个合理正确的路径，请检查自省，调整，测试，不跑冒烟，只跑真实的

上面通过了，进行vision部分的迁移适配，继续保持高效和质量。
vision部分，注意事项：GPTQModel需要重新量化，rotate需要考虑vision部分（至少vision的输出需要做与llm的embedding一样的rotate）。gptqmodel量化的模型需要重新量化，带上这个变换。可以先跑w8a8的验证正确性，然后再去gptqmodel那边重新量化。

好的，给我写好readme，然后再给我运行下浮点模型和量化模型导出的hmonnx，给出完整的结果，输出报告，让我交付